; EXERCISE 11: Bitwise NOT — Flipping Bits
; The NOT instruction flips every bit of a value (bitwise complement).
; On a 16-bit register, NOT AX means AX = ~AX.
;
; Load AX with 0x00FF, then apply NOT to it.
; AX should equal 0xFF00.
;
; ASSERT_REG: AX == 0xFF00
global _start
section .text
_start:
    ; Write your code here:
    ; I AM NOT DONE

    hlt
