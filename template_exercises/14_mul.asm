; EXERCISE 14: Unsigned Multiply (MUL)
; MUL multiplies AX by the given operand.
; For a 16-bit operand:  DX:AX = AX * operand
; (the 32-bit result is split across DX (high) and AX (low))
;
; Set AX to 0x0005 and BX to 0x0006, then multiply AX by BX.
; AX should equal 0x001E (30 decimal).  DX should be 0x0000.

global _start
section .text
_start:
    ; Write your code here:
    ; I AM NOT DONE
    
    hlt
