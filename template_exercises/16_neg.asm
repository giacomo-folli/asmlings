; EXERCISE 16: NEG — Two's Complement Negation
; NEG computes the two's complement negation: effectively does (0 - operand).
; It is the signed equivalent of "multiply by -1".
;
; Load AX with 0x0005. Negate it.
; In 16-bit two's complement, -5 is 0xFFFB.

global _start
section .text
_start:
    ; Write your code here:
    ; I AM NOT DONE
    
    hlt
